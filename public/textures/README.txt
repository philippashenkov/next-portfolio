Place high-resolution planet textures here to be served as /textures/*.
Expected filenames:
- earth.ktx2 (preferred if you have KTX2)
- earth.jpg  (fallback)

These are optional: the app will use a remote fallback if not present.
